# -------------------------------
# MySQL (CONECTADO A RAILWAY)
# -------------------------------
# Datos reales de tu panel de Railway (Puerto Público 59650)
spring.datasource.url=jdbc:mysql://turntable.proxy.rlwy.net:59650/railway?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=ASfUkPsEEHjEliiMhfyOkORiviKLuEqE
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# -------------------------------
# MongoDB (CONECTADO A ATLAS)
# -------------------------------
# Usando tu usuario 'admin' y clave 'admin123' en tu cluster de Atlas
spring.data.mongodb.uri=mongodb+srv://admin:admin123@clinica2.wetjhte.mongodb.net/clinica2?retryWrites=true&w=majority&appName=clinica2
spring.data.mongodb.database=clinica2

# -------------------------------
# Thymeleaf
# -------------------------------
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# -------------------------------
# Security (Acceso a la API)
# -------------------------------
spring.security.user.name=admin
spring.security.user.password=admin

# -------------------------------
# App & Server Configuration
# -------------------------------
# IMPORTANTE: server.port con ${PORT} permite que Render o Railway asignen el puerto
server.port=${PORT:8080}
spring.application.name=sistema-citas-medicas

# -------------------------------
# Upload Settings
# -------------------------------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# -------------------------------
# Errors
# -------------------------------
server.error.include-stacktrace=never
server.error.include-message=always
server.error.whitelabel.enabled=false

# -------------------------------
# Logs
# -------------------------------
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO